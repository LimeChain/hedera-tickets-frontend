// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyAs0020JDODfPLioNPC_NAgyBM5YJ6obPs",
    authDomain: "hedera-tickets.firebaseapp.com",
    databaseURL: "https://hedera-tickets.firebaseio.com",
    projectId: "hedera-tickets",
    storageBucket: "hedera-tickets.appspot.com",
    messagingSenderId: "906038431165",
    appId: "1:906038431165:web:9fbb5acd89248ab0f0d6d4"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
